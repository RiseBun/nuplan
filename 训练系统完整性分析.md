# 🔍 三层评估系统 - 完整性分析

## ✅ 当前状态：已完成核心实现

经过本次开发，三层评估系统的核心组件已全部实现。

---

## 📊 已完成 vs 未完成

### ✅ 已完成的部分

| 组件 | 状态 | 说明 |
|------|------|------|
| ConsistencyCriticModel 架构 | ✅ | 6 维度评估头 |
| 训练循环框架 | ✅ | train.py |
| 配置文件 | ✅ | train_consistency_mini.py |
| 评估脚本 | ✅ | eval_critic.py + evaluate_three_layers.py |
| FID/FVD/LPIPS 计算器 | ✅ | 用于 Layer 1 |
| DrivingWorld 集成 | ✅ | DrivingWorld Wrapper |
| **训练数据生成脚本** | ✅ | generate_futures_drivingworld.py |
| **标签计算脚本** | ✅ | compute_training_labels.py |
| **索引构建脚本** | ✅ | build_consistency_index.py |
| **Ranking 评估** | ✅ | evaluate_layer3 真实实现 |

### 🟡 可选的增强方向

| 组件 | 状态 | 说明 |
|------|------|------|
| 真实 nuPlan simulator 集成 | 🟡 | 当前 Ranking 评估基于样本内数据 |
| DrivingWorld 模型权重 | 🟡 | 需要下载预训练权重 |
| 大规模数据生成 | 🟡 | 当前为 debug 模式 |

---

## 🎯 您的真实需求

您想要的是：

```
训练一个 Consistency Critic 模型
    ↓
该模型能够：
1. 评估生成质量（Layer 1 的能力）
2. 评估 Action 一致性（Layer 2 的能力）
3. 评估驾驶合理性（Layer 3 的能力）
    ↓
最终证明：
这个模型的评分与 nuPlan 闭环性能的相关性 > FID/FVD
```

---

## 🎯 使用流程

### Phase 1: 数据准备

```bash
# Step 1: 生成训练数据（使用占位符 World Model）
python generate_futures_drivingworld.py \
    --output-dir generated_data \
    --num-scenes 100

# Step 2: 计算多维度标签
python compute_training_labels.py \
    --input-dir generated_data \
    --output-dir labeled_data \
    --compute-all

# Step 3: 构建训练索引
python build_consistency_index.py \
    --input-dir labeled_data \
    --output-dir indices_consistency
```

### Phase 2: 训练模型

```bash
# 本地训练
python train.py --config configs/train_consistency_mini.py

# DLC 训练
bash dlc_train.sh
```

### Phase 3: 评估模型

```bash
# 基础评估
python eval_critic.py --checkpoint work_dirs/consistency_mini_v1/checkpoints/best.pth

# Ranking 评估
python eval_critic.py --checkpoint work_dirs/consistency_mini_v1/checkpoints/best.pth --eval-ranking

# 三层评估
python evaluate_three_layers.py \
    --generated-dir generated_data \
    --critic-path work_dirs/consistency_mini_v1/checkpoints/best.pth \
    --ranking-groups indices_consistency/ranking_groups_val.json
```

---

## 📁 新增文件清单

| 文件 | 用途 |
|------|------|
| `generate_futures_drivingworld.py` | 使用 World Model 生成未来图像，构建训练数据 |
| `compute_training_labels.py` | 计算多维度标签（consistency, speed, steering, progress, temporal, validity）|
| `build_consistency_index.py` | 构建与 train.py 兼容的训练索引 |

---

## 🔧 新增评估能力

### evaluate_three_layers.py 改进

- `evaluate_layer3()` 现在实现了真实的 NDCG/MRR/Top-k Hit Rate 计算
- 支持从 ranking_groups 文件加载多候选场景数据
- 支持从数据目录自动检测多候选场景

### 使用示例

```bash
# 方法1: 使用 ranking_groups 文件
python evaluate_three_layers.py \
    --generated-dir generated_data \
    --critic-path checkpoints/best.pth \
    --ranking-groups indices_consistency/ranking_groups_val.json

# 方法2: 从数据目录自动检测
python evaluate_three_layers.py \
    --generated-dir labeled_data \
    --critic-path checkpoints/best.pth
```

---

## ✅ 训练系统完整性

经过本次开发，nuplan critic 训练系统已具备：

1. ✅ **数据生成流水线**: `generate_futures_drivingworld.py` → `compute_training_labels.py` → `build_consistency_index.py`
2. ✅ **训练框架**: train.py 支持 DDP 分布式训练
3. ✅ **多维度评估**: 6 个评估头（consistency, speed, steering, progress, temporal, validity）
4. ✅ **Ranking 评估**: NDCG@3/5, MRR, Top-1 Hit Rate
5. ✅ **三层评估系统**: Layer 1 (生成质量) + Layer 2 (Action 一致性) + Layer 3 (驾驶合理性)

**下一步**: 下载 DrivingWorld 预训练权重，进行真实数据生成和训练。
